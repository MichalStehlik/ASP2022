@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<ul>
    @foreach(var s in Model.Students) {
        <li>
            @s.Firstname @s.Lastname @s.Classroom.Name
        </li>
    }
</ul>
